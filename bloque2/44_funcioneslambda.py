# funciones lambda
# son funciones anonimas para tareas simples

# despues de lambda es el parametro y lo que devuelve
dime_el_year = lambda year: f"El a;o es: {year * 50}"

print(dime_el_year(2034))
